import NavBar from "./prueba";

const Footer = () => {
    return (
        <footer>
            <section>

            </section>
            <NavBar />
        </footer>
    );
}

export default Footer;